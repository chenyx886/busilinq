<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/iv_empty"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/empty_msg_tv"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="10dp"
        android:background="@mipmap/ic_to_developed" />

    <TextView
        android:id="@+id/empty_msg_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:text="暂无数据"
        android:textColor="@color/page_title_color"
        android:textSize="@dimen/text_size14" />

    <Button
        android:id="@+id/btn_purchase"
        android:layout_width="145.00dp"
        android:layout_height="40dp"
        android:layout_below="@id/iv_empty"
        android:layout_centerInParent="true"
        android:layout_gravity="center"
        android:layout_marginTop="45dp"
        android:background="@mipmap/ic_btn_login_bg"
        android:text="@string/start_to_buy"
        android:textColor="@android:color/white"
        android:textSize="@dimen/text_size18"
        android:visibility="gone" />
</RelativeLayout>